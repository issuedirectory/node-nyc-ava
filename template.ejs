FROM <%= FROM %>

RUN echo "Caching NPM files" \
<% if (standard) { -%>
  && npm cache add nyc \
  && npm cache add ava \
<% } -%>
<% if (full) { -%>
  && npm cache add bower \
  && npm cache add sinon \
  && npm cache add express \
<% } -%>
  echo "Finished caching files."
